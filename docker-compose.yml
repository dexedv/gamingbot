services:
  gamingbot:
    build: .
    container_name: gamingbot
    restart: always
    ports:
      - "80:5000"
    volumes:
      - ./data:/app/data        # Datenbank bleibt erhalten
    env_file:
      - .env
